# Progress Notification Example

Demonstrates **real-time progress notifications** during long-running tool operations.

## Features

- Server sends progress updates to connected clients
- Client receives and displays progress in real-time
- Examples with and without progress tokens
- Proper progress token handling

## Build and Run

```bash
cmake --build build --target progress_example
.\build\examples\progress_example.exe
```
